<h1>編集画面</h1>

<%= form_with model: @item, url: admin_item_path(@item.id), method: :patch do |f| %>

  <h4>タイトル</h4>
  <%= f.text_field :name %>

  <h4>本文</h4>
  <%= f.text_area :description %>

  <%= f.submit '保存' %>

<% end %>