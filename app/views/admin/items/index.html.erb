<h1>商品一覧</h1>

<% @items.each do |item| %>

  <p>タイトル</p>
  <span><%= link_to item.name, admin_item_path(item.id) %></span>
  <span><%= item.description %></span>
  <span><%= item.price %></span>
<% end %>