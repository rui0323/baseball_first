<h1>顧客用　商品一覧</h1>

 <% @items.each do |item| %>
  <%= link_to item.name, item_path(item.id)  %>
  <span><%= item.description %></span>
   ￥<%= item.price %>
    <span><% if item.is_active %>
 <p>販売中</p>
 <% else %>
 <p>販売停止中</p>
 <% end %>
 </span>
  <% end %>