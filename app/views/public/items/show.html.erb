<h1>商品詳細</h1>
 <%= image_tag @item.image ,size: "200x200" %>
<%= @item.name %>
<%= @item.description %>
￥<%= @item.price %> (税込)
 <span><% if @item.is_active %>
 <p>販売中</p>
 <% else %>
 <p>販売停止中</p>
 <% end %>
 </span>
  <p>発売日：<%= @item.created_at.strftime('%Y/%m/%d') %></p>