 <h2>注文情報確認</h2>

  <% current_customer.cart_items.each do |cart_item| %>

   <%= cart_item.item.name %>
   <%= cart_item.item.with_tax_price %>
   <%= cart_item.amount %>
   <%= cart_item.subtotal %></br>
  
  <% end %>
   <h4>支払方法</h4>
  <%= @order.payment_method %>
   <h4>お届け先</h4>
   〒<%= @order.postcode %><%= @order.address %>
   <%= @order.name %>
   
   <h4>送料</h4>
   <%= @order.shipping_fee %>
   <h4>商品合計</h4>
   <%= @cart_item_total_price %>
    <h4>請求金額</h4>
    <%= @cart_item_total_price + @order.shipping_fee %>